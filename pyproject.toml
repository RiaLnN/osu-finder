[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "osu-finder"
version = "1.3.2"
description = "Automated osu! beatmap finder — local PP/SR calculation via rosu-pp-py, mod-aware filtering, and profile-based preset generation."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Your Name" }]
keywords = ["osu", "beatmap", "rosu-pp", "rhythm-game"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Games/Entertainment",
]
dependencies = [
    "aiohttp>=3.9",
    "PyYAML>=6.0",
    "rosu-pp-py>=4.0",
    "rich>=13.0",
    "pytest>=8",
    "pytest-asyncio",
    "pytest-cov",
    "aioresponses"
]

[project.urls]
Homepage = "https://github.com/RiaLnN/osu-finder"
Issues = "https://github.com/RiaLnN/osu-finder/issues"

[project.scripts]
osu-finder = "osu_finder.main:main"

[tool.setuptools.packages.find]
include = ["osu_finder*"]